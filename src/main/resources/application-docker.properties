# Sobrescreve datasource para ambiente Docker (host = nome do serviço no compose)
spring.datasource.url=jdbc:postgresql://postgres:5432/condominio
spring.datasource.username=condominio
spring.datasource.password=condominio123

# Diretório de upload dentro do container
app.upload.dir=/uploads
